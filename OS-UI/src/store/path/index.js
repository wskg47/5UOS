
const state = {
  routerList: []
}
const mutations = {
  initRouters(state, data) {
    state.routerList = data
  }
}

const actions = {
}

const getters = {}

export default {
  state, mutations, actions, getters
}